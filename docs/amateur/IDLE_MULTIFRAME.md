# Amateur — Multi-frame idle (Support 2)

Beyond keyed stills: `assets/binds/idle/sheets/<Slug>.png` are 8-frame horizontal A-pose breathe sheets for Elara + Amateur 12.

While free/idle, AI billboards advance the sheet (~6–10 fps from bind) with light sway. Walk/run/tickle/laugh fall back to the keyed still (or clip frames).

**Plaza third-person:** the hub mannequin uses the same sheets while you walk the plaza (over-shoulder cam). Look / loadout still come from persist (`tb-amateur-save`).
