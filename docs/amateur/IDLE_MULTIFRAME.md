# Amateur — Multi-frame idle (Support 2)

Beyond keyed stills: `assets/binds/idle/sheets/<Slug>.png` are 8-frame horizontal A-pose breathe sheets for Elara + Amateur 12.

While free/idle, AI + plaza mannequin billboards use smoothstep sheet crossfade plus punchier dual-phase breathe / weight-shift (past #36). Plaza mannequin **tracks feet tightly** in the hall walk, then **eases softer** into Home/Shop pose so idle phase stays continuous.

Look swaps clear the prior sheet. Persist: equipped loadout stays owned; mannequin picks up skill-block changes after spend.
