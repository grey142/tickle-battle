# Amateur — Idle A-pose binds (Support 2)

Per-look `assets/binds/idle/<Slug>.idle.json` for Elara Case + Amateur 12.

Drives billboard breathe / sway / scale pulse while free (A-pose idle). Tickle/laugh clips stay on their own lanes.

Also: save now persists `xp` with coins/level/unspent/blocks/look/weapon/armor.
