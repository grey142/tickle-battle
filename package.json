{
  "name": "tickle-battle",
  "private": true,
  "version": "0.1.0",
  "description": "Playable Amateur Team Quick prototype for Tickle Battle (first-person 6v6 vs AI).",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/grey142/tickle-battle.git"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 43180",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 43180"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
